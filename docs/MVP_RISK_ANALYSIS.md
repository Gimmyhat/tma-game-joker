# Анализ рисков MVP (статический обзор)

Дата: 2026-01-30
Область: Telegram Mini App Joker (frontend, backend, shared)
Метод: только статический обзор кода (без тестов выполнения и прод-телеметрии)

## Краткое резюме

- Фокус приоритетов: привязка идентичности/аутентификации, защита от повторов, устойчивость авторитетного состояния.
- Текущий дизайн пригоден для однопроцессного MVP, но уязвим при рестартах, масштабировании и ошибках конфигурации.
- Ряд рисков чувствителен к настройкам (SKIP_AUTH, CORS, опциональный Redis) и может стать критическим в продакшене.

## Определения приоритетов

- P0: обязательно исправить до масштабирования или монетизации; риски безопасности или потери данных.
- P1: высокий удар по надежности/доступности; исправить вскоре после P0.
- P2: средний удар по UX/операциям; исправлять в плановом режиме.
- P3: низкий удар или техдолг инструментов; исправлять по возможности.

## Выявленные проблемы (по приоритету)

### P0-1: Идентичность не привязана к проверенному Telegram initData

- Риск: валидное initData можно сопоставить с произвольным userId в WS query.
- Влияние: подмена аккаунтов, компрометация честности игры.
- Доказательства: `apps/backend/src/gateway/game.gateway.ts`, `apps/backend/src/auth/guards/telegram-auth.guard.ts`, `apps/frontend/src/lib/socket.ts`

### P0-2: Нет защиты от повторного использования initData

- Риск: initData принимается без проверки TTL по auth_date.
- Влияние: повторные сессии остаются валидными дольше положенного.
- Доказательства: `apps/backend/src/auth/guards/telegram-auth.guard.ts`

### P0-3: SKIP_AUTH может отключить аутентификацию в проде

- Риск: неверная конфигурация (SKIP_AUTH=true) полностью отключает auth.
- Влияние: несанкционированный доступ при ошибочном деплое.
- Доказательства: `apps/backend/src/auth/guards/telegram-auth.guard.ts`, `apps/frontend/playwright.config.ts`, `PROGRESS.md`

### P0-4: Авторитетное состояние в памяти, Redis опционален

- Риск: рестарты стирают игры/очереди; при нескольких инстансах состояние рассинхронизируется.
- Влияние: потеря матчей, неконсистентные комнаты, невозможность масштабирования.
- Доказательства: `apps/backend/src/game/services/room.manager.ts`, `apps/backend/src/database/redis.service.ts`

### P0-5: Отсутствие rate limiting на game actions ⚠️ НОВЫЙ

- Риск: игроки могут отправлять неограниченное количество ходов/ставок в секунду.
- Влияние: DoS-атака на сервер, спам ходами, нарушение игрового процесса.
- Доказательства: `apps/backend/src/gateway/game.gateway.ts` (есть rate limit только на throw_card: 300ms, но нет глобального limiting'а)
- **Рекомендация:** Добавить rate limiting на все game actions (find_game, make_bet, select_trump, throw_card) - максимум 10 действий/минута на пользователя.

### P1-1: Таймеры процесс-локальные, без восстановления

- Риск: после рестарта игры зависают или таймеры сбрасываются некорректно.
- Влияние: зависшие комнаты и сломанные сценарии реконнекта.
- Доказательства: `apps/backend/src/game/services/game-process.service.ts`

### P1-2: Логика freeze/resume при 1 человеке может остановить игру

- Риск: таймеры заморожены при одном человеке, но нет явного резюма при повторном входе.
- Влияние: игра может не продолжиться после реконнекта.
- Доказательства: `apps/backend/src/game/services/game-process.service.ts`

### P1-3: CORS допускает любой origin при credentials

- Риск: wildcard origin с credentials расширяет поверхность атак.
- Влияние: риск кросс-ориджин злоупотреблений и утечки токенов.
- Доказательства: `apps/backend/src/main.ts`, `apps/backend/src/gateway/game.gateway.ts`

### P2-1: Health check не проверяет зависимости

- Риск: endpoint всегда возвращает ok.
- Влияние: оркестраторы не замечают падений Redis/Postgres.
- Доказательства: `apps/backend/src/health/health.controller.ts`

### P2-2: Аудит игры зависит от Redis

- Риск: при падении Redis логи игры не доходят до Postgres.
- Влияние: потеря аудита и аналитики.
- Доказательства: `apps/backend/src/game/services/game-audit.service.ts`, `apps/backend/src/database/redis.service.ts`

### P2-3: TTL состояния игры может истечь при долгой паузе

- Риск: TTL Redis 2 часа и может истечь во время заморозки.
- Влияние: реконнекты теряют состояние.
- Доказательства: `apps/backend/src/database/redis.service.ts`, `apps/backend/src/game/services/game-process.service.ts`

### P2-4: Отладочные логи всегда включены в горячих путях

- Риск: высокий объем логов, утечки данных, нагрузка.
- Влияние: рост стоимости наблюдаемости и риск PII.
- Доказательства: `apps/backend/src/gateway/game.gateway.ts`, `apps/frontend/src/store/gameStore.ts`

### P2-5: Клиент только WebSocket, без fallback

- Риск: WS блокируется или нестабилен у части сетей.
- Влияние: пользователи не могут подключиться.
- Доказательства: `apps/frontend/src/lib/socket.ts`

### P2-6: Socket listeners не очищаются при повторной инициализации

- Риск: дублирование обработчиков событий.
- Влияние: двойные обновления и неконсистентный UI.
- Доказательства: `apps/frontend/src/store/gameStore.ts`

### P2-7: Клиент сбрасывает состояние до ack от сервера

- Риск: расхождение UI и сервера при ошибке leave.
- Влияние: призрачные игроки и рассинхрон лобби.
- Доказательства: `apps/frontend/src/store/slices/gameSlice.ts`

### P2-8: Детектор Telegram может ошибиться в проде

- Риск: при сбое детекта используется mock initData и auth падает.
- Влияние: непроходимые ошибки подключения на крайних клиентах.
- Доказательства: `apps/frontend/src/providers/TelegramProvider.tsx`, `apps/frontend/src/lib/telegram.ts`, `apps/frontend/src/lib/socket.ts`

### P2-9: Аудит хранит полные руки и детальные данные

- Риск: рост объема хранилища и чувствительные данные.
- Влияние: увеличение стоимости и риск приватности.
- Доказательства: `apps/backend/src/game/services/game-engine.service.ts`, `apps/backend/src/game/services/game-audit.service.ts`

### P3-1: Флейковые тесты таймингов

- Риск: нестабильный CI указывает на чувствительность к таймерам.
- Влияние: замедление поставки и снижение доверия к тестам.
- Доказательства: `apps/backend/test/resilience.e2e-spec.ts`

## Точечный план исправлений

### Фаза 0 (P0): безопасность и авторитетное состояние (блокеры)

#### 1. Привязать идентичность к initData, игнорировать userId из query
- Парсить пользователя из initData в guard, сохранять проверенный userId в socket data.
- Обновить gateway, чтобы использовать socket data для id и name.
- **Критерий приемки:** несовпадающий userId в query игнорируется или отклоняется.
- **Статус:** 🔄 Требует исправления

#### 2. Добавить защиту от повторов initData
- Проверять TTL auth_date (настраиваемо, например 5-15 минут).
- **Критерий приемки:** auth_date вне TTL отклоняется.
- **Статус:** 🔄 Требует исправления

#### 3. Ужесточить SKIP_AUTH
- Разрешить только при NODE_ENV=development или явном ALLOW_DEV_AUTH=true.
- Падать при старте или явно предупреждать, если включено в проде.
- **Критерий приемки:** в проде SKIP_AUTH не может обойти auth.
- **Статус:** 🔄 Требует исправления

#### 4. Требовать Redis в проде
- Ввести флаг REQUIRE_REDIS и падать или отключать матчмейкинг при недоступности Redis.
- Документировать ограничения однопроцессного режима в dev.
- **Критерий приемки:** прод стартует только при доступном Redis (или в режиме обслуживания).
- **Статус:** 🔄 Требует исправления

#### 5. Добавить rate limiting на game actions ⚠️ НОВЫЙ
- Добавить middleware/guard для rate limiting на все game actions.
- Ограничить частоту ходов (например, max 10 actions/минута на пользователя).
- **Критерий приемки:** чрезмерная активность блокируется.
- **Статус:** 🔄 Требует исправления

### Фаза 1 (P1): надежность таймеров и безопасность транспорта

#### 1. Восстанавливать таймеры после рестарта
- Сохранять дедлайны в GameState и пересоздавать таймеры при восстановлении комнаты.
- **Критерий приемки:** после рестарта таймеры хода и recap продолжаются корректно.
- **Статус:** ⏳ Может отложить до после MVP

#### 2. Исправить freeze/resume
- При реконнекте, если людей > 1, размораживать и перезапускать таймеры.
- **Критерий приемки:** комната автоматически продолжается после возврата второго игрока.
- **Статус:** ⏳ Может отложить до после MVP

#### 3. Ограничить CORS origins
- Использовать явный список разрешенных origin для HTTP и WS.
- **Критерий приемки:** подключение возможно только с разрешенных доменов.
- **Статус:** ⏳ Может отложить до после MVP

### Фаза 2 (P2): операции, UX и стабильность

#### 1. Улучшить health endpoint
- Добавить проверки Redis ping и подключения к БД.
- **Критерий приемки:** health показывает деградацию при падении зависимостей.
- **Статус:** ⏳ Не критично для MVP

#### 2. Фолбек аудита при падении Redis
- Вариант A: писать минимальные записи напрямую в Postgres.
- Вариант B: буферизовать в памяти с лимитами и позже сбрасывать.
- **Критерий приемки:** завершенные игры сохраняются даже без Redis.
- **Статус:** ⏳ Не критично для MVP

#### 3. Продлить или обновлять TTL
- Обновлять TTL при обновлениях состояния и во время freeze.
- **Критерий приемки:** долгие игры не истекают в Redis.
- **Статус:** ⏳ Не критично для MVP

#### 4. Гейтировать debug-логи
- Добавить флаг DEBUG_LOGS для gateway и store.
- **Критерий приемки:** в проде минимальные логи по умолчанию.
- **Статус:** ⏳ Не критично для MVP

#### 5. Фолбек подключения и UX ошибок
- Разрешить polling fallback или показывать четкую ошибку при сбое WS.
- **Критерий приемки:** пользователь получает рабочий путь подключения или понятную ошибку.
- **Статус:** ⏳ Не критично для MVP

#### 6. Чистить socket listeners при реинициализации
- Гарантировать единичную подписку или удалять при reset.
- **Критерий приемки:** нет дублирующих обработчиков.
- **Статус:** ⏳ Не критично для MVP

#### 7. Ждать ack сервера при leave
- Сбрасывать состояние только после подтверждения сервера.
- **Критерий приемки:** UI и серверное состояние согласованы.
- **Статус:** ⏳ Не критично для MVP

#### 8. Улучшить поведение при сбое детекта Telegram
- В проде при отсутствии Telegram показывать ошибку, а не mock.
- **Критерий приемки:** доступ вне Telegram блокируется с инструкцией.
- **Статус:** ⏳ Не критично для MVP

#### 9. Сократить payload аудита
- Не логировать полные руки или хранить редактированный вариант.
- **Критерий приемки:** размер аудита уменьшен без потери ключевых событий.
- **Статус:** ⏳ Не критично для MVP

### Фаза 3 (P3): надежность тестов

#### 1. Стабилизировать тайминговые тесты
- Использовать fake timers или детерминированное время в E2E.
- **Критерий приемки:** resilience тесты стабильны в CI.
- **Статус:** ⏳ Не критично для MVP

## Рекомендуемые следующие шаги

1. **Немедленно (P0):** Исправить все 5 P0-рисков перед запуском MVP
   - P0-1: Привязка идентичности к initData
   - P0-2: Защита от повторного использования initData
   - P0-3: Ужесточение SKIP_AUTH
   - P0-4: Обязательный Redis для продакшена
   - P0-5: Rate limiting на game actions

2. **После MVP (P1):** Реализовать восстановление таймеров и ограничение CORS

3. **В плановом порядке (P2-P3):** Остальные улучшения по мере необходимости

## Примечания

- Отчет основан только на статическом обзоре кода.
- Изменения в код не вносились.
- **Важно:** P0-5 (rate limiting) был добавлен как новый критичный риск, отсутствующий в первоначальном анализе.
