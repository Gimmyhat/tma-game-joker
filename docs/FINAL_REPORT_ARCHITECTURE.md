 # Итоговый отчет по архитектуре проекта: TMA Game Joker

## 1. Общая концепция: Монорепозиторий

Проект реализован как **Монорепозиторий (Monorepo)** на базе **pnpm workspace**. Это значит, что серверная часть (Backend), клиентская часть (Frontend), админ-панель (Admin) и общие правила игры хранятся и версионируются вместе. Это исключает рассинхронизацию логики: сервер и клиент всегда "играют по одним правилам".

Архитектура разделена на **четыре ключевых модуля**:

### 🟧 Shared (Общее ядро)

- **Что это:** Общая библиотека кода — **единый источник правил игры**.
- **Где живет:** `packages/shared`
- **Содержит:**
  - Типы данных (Card, Player, GameState)
  - Правила игры (`canPlayCard()`, `determineTrickWinner()`)
  - Валидация (`isValidBid()`, `isValidPlay()`)
- **Зачем нужен:**
  - Frontend импортирует правила → подсвечивает доступные карты, показывает подсказки
  - Backend импортирует **те же самые** правила → проверяет легитимность ходов, защищает от читеров
  - Код **идентичен** — невозможна ситуация, когда клиент разрешает ход, а сервер отклоняет

> **Аналогия:** Представь Shared как **книгу правил** игры Joker. У игрока (Frontend) — копия для подсказок, у судьи (Backend) — копия для арбитража. Обе копии идентичны, потому что это одна и та же книга.

### 🟦 Frontend (Клиентская часть)

- **Что это:** "Лицо" игры внутри Telegram.
- **Где живет:** `apps/frontend`
- **Технологии:**
  - **React 18** — UI-фреймворк
  - **Vite** — сборщик
  - **Zustand** — state management
  - **TailwindCSS** — стилизация
  - **Framer Motion** — анимации карт и интерфейса
  - **i18next** — мультиязычность (RU/EN)
  - **Telegram Apps SDK** — интеграция с Telegram Mini Apps
  - **Playwright** — E2E тестирование
- **Роль:** Отвечает за интерфейс, анимации карт, интеграцию с Telegram SDK и отображение состояния игры. Он "глупый" — он не принимает решений, он только отображает то, что прислал сервер, и отправляет намерения игрока.

### 🟩 Backend (Серверная часть)

- **Что это:** "Мозг" и "Арбитр" игры.
- **Где живет:** `apps/backend`
- **Технологии:**
  - **NestJS** — серверный фреймворк
  - **Socket.io** — WebSocket-коммуникация
  - **Redis (ioredis)** — кэширование и хранение активных игр
  - **Prisma ORM** — работа с персистентной базой данных
  - **Grammy** — Telegram Bot API для уведомлений
- **Роль:** Управляет комнатами, таймерами, ботами, сохраняет прогресс и историю игр.

### 🟪 Admin Panel (Административная панель)

- **Что это:** Интерфейс управления данными для администраторов.
- **Где живет:** `apps/admin`
- **Технологии:**
  - **AdminJS** — low-code админ-панель
  - **Express** — HTTP-сервер
  - **Prisma** — ORM для работы с БД
- **Роль:** Позволяет просматривать и редактировать данные пользователей, игр, статистики без прямого доступа к БД.

---

## 2. Где и как хранятся данные?

### Персистентные данные (Database)

- **Что хранится:** Профили пользователей, история игр, статистика, достижения.
- **Технология:** Реляционная БД через **Prisma ORM**.
- **Схема:** Определена в `apps/backend/prisma/schema.prisma`.

### Оперативные данные (State)

- **Состояние активных игр:** Хранится в **Redis** (быстрая in-memory база данных).
- **Почему так:** Joker — игра динамичная. Redis позволяет мгновенно обновлять состояние стола (миллисекунды) и восстанавливать игру, если сервер перезагрузится.
- **Жизненный цикл:** Игра создается в памяти → каждое действие обновляет запись в Redis → после окончания партии результаты фиксируются в БД, а временные данные очищаются.

### Взаимодействие (Communication)

Все общение происходит через **WebSockets (Socket.io)**. Это не классические HTTP-запросы (как при загрузке сайтов), а постоянный открытый канал связи.

**Схема потока данных при ходе игрока:**

1.  **Игрок:** Кидает карту (Frontend проверяет валидность через `Shared` модуль).
2.  **Сеть:** Событие летит по сокету на сервер (`GameGateway`).
3.  **Логика:** Сервер (`GameProcessService`) принимает ход, проверяет его легитимность.
4.  **Расчет:** Движок пересчитывает стол, определяет, чья взятка, обновляет очки.
5.  **Сохранение:** Новое состояние игры мгновенно пишется в **Redis**.
6.  **Ответ:** Сервер рассылает обновленное поле всем игрокам в комнате одновременно.

---

## 3. Инфраструктура

### Docker

Проект контейнеризирован через **Docker Compose**:

- `docker-compose.yml` — локальная разработка (Redis, DB)
- `docker-compose.prod.yml` — production-окружение
- `nginx/` — конфигурация reverse proxy

### CI/CD

- GitHub Actions (`.github/`) — автоматизация сборки и деплоя

---

## 4. Ключевые архитектурные решения (V2)

1.  **Чистая логика (Pure Logic):** Вся математика игры вынесена в чистые функции. Они не зависят от базы данных или интернета. Их легко тестировать и невозможно "сломать" сетевыми лагами.
2.  **Bot Strategy Pattern:** Логика ботов отделена от движка. Боты "смотрят" на игру так же, как люди, и принимают решения на основе текущего состояния стола. Если игрок вылетел/таймаут — сервер подменяет его ботом бесшовно.
3.  **Telegram Integration:** Приложение знает, что оно запущено в Telegram. Оно использует нативные цвета темы пользователя и данные профиля для авторизации, но при этом умеет работать в браузере (для отладки) через специальный dev-режим.
4.  **Двухуровневое хранение:** Redis для "горячих" данных активных игр, Prisma/DB для персистентной истории и профилей.

---

## Резюме

Система построена надежно и современно. Логика дублируется на клиенте (для скорости) и на сервере (для безопасности), но исходный код для нее — единый. Данные "горячих" игр живут в быстрой памяти (Redis), что обеспечивает мгновенный отклик. Персистентные данные хранятся в реляционной БД через Prisma. Масштабируемость заложена архитектурно: можно запускать много копий игры параллельно.
